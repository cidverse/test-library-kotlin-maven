
# cid-workflow-version: 0.9.0

# This file is generated by the CID Workflow GitLab App.
# DO NOT EDIT!

# global defaults
default:
  image: quay.io/podman/stable@sha256:42d8b3d789333f07e631305325a02b4ab33c3f107413e04b612973a72f1d7deb # v5.8.1-immutable
  before_script:
    - bash .cid/scripts/install.sh "0.9.0" ""

# variables
variables:
  # git settings
  GIT_STRATEGY: clone
  GIT_DEPTH: 10

# stages
stages:
    - build
    - test
    - scan
    - publish

# configuration snippets
.default-job: &default_job
  
  tags:
    - saas-linux-small-amd64
  allow_failure: false
  interruptible: true
  retry:
    max: 2
    when:
      - runner_system_failure
      - api_failure
      - scheduler_failure

# Nightly
## maven-build [test-library-kotlin-maven]
"nightly/maven-build [test-library-kotlin-maven]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "maven-build-test-library-kotlin-maven"
    artifacts:
      name: "nightly/maven-build [test-library-kotlin-maven]"
      paths:
        - .dist/maven-build-test-library-kotlin-maven/
      expire_in: 1 day
## maven-test [test-library-kotlin-maven]
"nightly/maven-test [test-library-kotlin-maven]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "maven-test-test-library-kotlin-maven"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "nightly/maven-test [test-library-kotlin-maven]"
      paths:
        - .dist/maven-test-test-library-kotlin-maven/
      expire_in: 1 day
      reports:
        junit: .dist/maven-test-test-library-kotlin-maven/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/maven-test-test-library-kotlin-maven/report/jacoco/*.xml
## gitleaks-scan
"nightly/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "gitleaks-scan"
    artifacts:
      name: "nightly/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"nightly/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "semgrep-scan"
    artifacts:
      name: "nightly/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"nightly/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "schedule" && $CI_PIPELINE_SCHEDULE_DESCRIPTION == "nightly"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "nightly" --step "trivyfs-scan"
    artifacts:
      name: "nightly/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Pull Request
## maven-build [test-library-kotlin-maven]
"pull-request/maven-build [test-library-kotlin-maven]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "maven-build-test-library-kotlin-maven"
    artifacts:
      name: "pull-request/maven-build [test-library-kotlin-maven]"
      paths:
        - .dist/maven-build-test-library-kotlin-maven/
      expire_in: 1 day
## maven-test [test-library-kotlin-maven]
"pull-request/maven-test [test-library-kotlin-maven]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "maven-test-test-library-kotlin-maven"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "pull-request/maven-test [test-library-kotlin-maven]"
      paths:
        - .dist/maven-test-test-library-kotlin-maven/
      expire_in: 1 day
      reports:
        junit: .dist/maven-test-test-library-kotlin-maven/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/maven-test-test-library-kotlin-maven/report/jacoco/*.xml
## gitleaks-scan
"pull-request/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "gitleaks-scan"
    artifacts:
      name: "pull-request/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"pull-request/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "semgrep-scan"
    artifacts:
      name: "pull-request/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"pull-request/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "pull-request" --step "trivyfs-scan"
    artifacts:
      name: "pull-request/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json

# Release
## maven-build [test-library-kotlin-maven]
"release/maven-build [test-library-kotlin-maven]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "maven-build-test-library-kotlin-maven"
    artifacts:
      name: "release/maven-build [test-library-kotlin-maven]"
      paths:
        - .dist/maven-build-test-library-kotlin-maven/
      expire_in: 1 day
## maven-test [test-library-kotlin-maven]
"release/maven-test [test-library-kotlin-maven]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "maven-test-test-library-kotlin-maven"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "release/maven-test [test-library-kotlin-maven]"
      paths:
        - .dist/maven-test-test-library-kotlin-maven/
      expire_in: 1 day
      reports:
        junit: .dist/maven-test-test-library-kotlin-maven/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/maven-test-test-library-kotlin-maven/report/jacoco/*.xml
## gitleaks-scan
"release/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitleaks-scan"
    artifacts:
      name: "release/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"release/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "semgrep-scan"
    artifacts:
      name: "release/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"release/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "trivyfs-scan"
    artifacts:
      name: "release/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
## changelog-generate
"release/changelog-generate":
    <<: *default_job
    stage: publish
    needs: ["release/gitleaks-scan", "release/maven-build [test-library-kotlin-maven]", "release/maven-test [test-library-kotlin-maven]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "changelog-generate"
    artifacts:
      name: "release/changelog-generate"
      paths:
        - .dist/changelog-generate/
      expire_in: 1 day
## gitlab-release-publish
"release/gitlab-release-publish":
    <<: *default_job
    stage: publish
    needs: ["release/changelog-generate", "release/gitleaks-scan", "release/maven-build [test-library-kotlin-maven]", "release/maven-test [test-library-kotlin-maven]", "release/semgrep-scan", "release/trivyfs-scan"]
    dependencies: ["release/changelog-generate"]
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TAG =~ /^v[0-9]+.[0-9]+.[0-9]+$/'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "release" --step "gitlab-release-publish"
    artifacts:
      name: "release/gitlab-release-publish"
      paths:
        - .dist/gitlab-release-publish/
      expire_in: 1 day

# Main
## maven-build [test-library-kotlin-maven]
"main/maven-build [test-library-kotlin-maven]":
    <<: *default_job
    stage: build
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "maven-build-test-library-kotlin-maven"
    artifacts:
      name: "main/maven-build [test-library-kotlin-maven]"
      paths:
        - .dist/maven-build-test-library-kotlin-maven/
      expire_in: 1 day
## maven-test [test-library-kotlin-maven]
"main/maven-test [test-library-kotlin-maven]":
    <<: *default_job
    stage: test
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "maven-test-test-library-kotlin-maven"
    coverage: /Test-Coverage:(\d+\.\d+)%/ # used for percentage
    artifacts:
      name: "main/maven-test [test-library-kotlin-maven]"
      paths:
        - .dist/maven-test-test-library-kotlin-maven/
      expire_in: 1 day
      reports:
        junit: .dist/maven-test-test-library-kotlin-maven/report/junit/*.xml
        coverage_report: # used for visualization only
          coverage_format: jacoco
          path: .dist/maven-test-test-library-kotlin-maven/report/jacoco/*.xml
## gitleaks-scan
"main/gitleaks-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "gitleaks-scan"
    artifacts:
      name: "main/gitleaks-scan"
      paths:
        - .dist/gitleaks-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/gitleaks-scan/report/gl-codequality/*.json
## semgrep-scan
"main/semgrep-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "semgrep-scan"
    artifacts:
      name: "main/semgrep-scan"
      paths:
        - .dist/semgrep-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/semgrep-scan/report/gl-codequality/*.json
## trivyfs-scan
"main/trivyfs-scan":
    <<: *default_job
    stage: scan
    needs: []
    dependencies: []
    timeout: 10m
    rules:
      - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_BRANCH == "main"'
        when: always
      - when: never
    script:
        - cid --log-level=${CID_LOGLEVEL:-info} plan execute --state-file ".cid/state-gitlab.json" --state-wf-name "main" --step "trivyfs-scan"
    artifacts:
      name: "main/trivyfs-scan"
      paths:
        - .dist/trivyfs-scan/
      expire_in: 1 day
      reports:
        codequality: .dist/trivyfs-scan/report/gl-codequality/*.json
